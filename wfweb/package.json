{
  "name": "wfweb",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Editor de watchface do CMF Watch Pro 2 — 100% no navegador (lê/edita .bin).",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "build:deploy": "tsc --noEmit && vite build && rm -rf dist/corpus dist/*.bin",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/node": "^26.1.0",
    "typescript": "^5.5.4",
    "vite": "^5.4.0",
    "vitest": "^2.0.5"
  }
}
